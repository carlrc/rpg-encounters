# Frontend

## Setup

Install [nvm](https://github.com/nvm-sh/nvm), set the Node version defined in `package.json` and then install dependencies

```bash
npm ci
```

Build

```bash
NODE_ENV=production npm run build
```
